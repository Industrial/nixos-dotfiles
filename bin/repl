#!/usr/bin/env bash
echo "> repl"
exec > >(tee ./logs/repl) 2>&1
set -e

dotfiles_root="$(cd "$(dirname "$0")/.." && pwd)"
echo "> repl > cd $dotfiles_root"
cd "$dotfiles_root"
nix repl --extra-experimental-features "flakes repl-flake" ".#"
