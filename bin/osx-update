#!/usr/bin/env bash
set -e

HOST=${1:-"smithja"}
echo "bin/update > host: ${HOST}"

USER=${2:-"twieland"}
echo "bin/update > user: ${USER}"

bin/update-neovim
bin/update-zellij
# bin/update-hardware-configuration "${HOST}"
bin/update-nix-env
bin/update-flake
bin/osx-update-darwin-rebuild-flake "${HOST}"
bin/update-home-manager "${HOST}" "${USER}"
bin/osx-update-login-shell
