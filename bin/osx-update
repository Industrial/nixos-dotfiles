#!/usr/bin/env bash
set -e

source lib/lowercase.bash

HOST=$(lowercase ${1:-"smithja"})
echo "bin/osx-update > host: ${HOST}"

USER=$(lowercase ${2:-$USER})
echo "bin/osx-update > user: ${USER}"

bin/update-neovim
bin/update-zellij
# bin/update-hardware-configuration "${HOST}"
bin/update-nix-env
bin/update-flake
bin/osx-update-darwin-rebuild-flake "${HOST}"
features/home/programming/vscode/bin/clear-cache
bin/update-home-manager "${HOST}" "${USER}"
bin/update-login-shell "${USER}"
