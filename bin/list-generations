#!/usr/bin/env bash
set -e
exec > >(tee ./logs/list-generations) 2>&1

# shellcheck source=lib/lowercase.bash
source lib/lowercase.bash

# Use shell globbing to list matching files
for file in /nix/var/nix/profiles/system-*; do
    if [[ -e "$file" ]]; then
        basename "$file"
    fi
done
