#!/usr/bin/env bash
set -e

USER=${1:-"tom"}
echo "bin/osx-update-login-shell > user: ${USER}"

echo "bin/osx-update-login-shell > chsh -s /run/current-system/sw/bin/fish"
sudo chsh -s /run/current-system/sw/bin/fish "${USER}"