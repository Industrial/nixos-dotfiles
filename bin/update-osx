#!/usr/bin/env bash
set -e

source lib/lowercase.bash

HOST=$(lowercase ${1:-"smithja"})
echo "bin/update-osx > host: ${HOST}"

USER=$(lowercase ${2:-$USER})
echo "bin/update-osx > user: ${USER}"

bin/update-neovim
bin/update-zellij
# bin/update-hardware-configuration "${HOST}"
bin/update-nix-env
bin/update-flake
bin/update-osx-darwin-rebuild-flake "${HOST}"
features/home/programming/vscode/bin/clear-cache
bin/update-login-shell "${USER}"
