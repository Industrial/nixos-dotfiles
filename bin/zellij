#!/usr/bin/env bash
echo "> zellij"
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
PROJECT_ROOT="$(cd "$SCRIPT_DIR/.." && pwd)"

zellij \
  --config "$PROJECT_ROOT/zellij.kdl" \
  --layout "$PROJECT_ROOT/zellij-layout.kdl"
