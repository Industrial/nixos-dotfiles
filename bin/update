#!/usr/bin/env bash
# Stop on error
set -e

# Update channels
sudo nix-channel --update

# Build the system
sudo nixos-rebuild build --flake '.#drakkar'
sudo ./result/activate
rm -f ./result

# Build HomeManager
nix build '.#homeConfigurations.tom.activationPackage'
sudo ./result/activate
rm -f ./result
