# Mode: ORIENT

`[ID:ORIENT]`

## Goal

Sharpen the ask; pick skills and agent; set lane.

## Steps

1. Restate the ask in its sharpest correct form (`/quality`).
2. Scan `.cursor/skills/`; list **reviewed** vs **using** (`/skills`).
3. Select agent rule: researcher | architect | implementer | reviewer (`/agent`).
4. Set lane via `maestro intake` or estimate ([lanes.md](../lanes.md)).
5. Load [PROTOCOL.md](../PROTOCOL.md) hard rails.

## Writes

None.

## Exit

Satisfy [orient-exit.md](../checklists/orient-exit.md). Then:

- `tiny` + clear → RESEARCH (brief) or EXECUTE if files known
- else → RESEARCH
