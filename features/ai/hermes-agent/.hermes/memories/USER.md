User values concise, direct responses and verification of changes. Prefers NixOS configuration work without Home Manager. Likes class-level skill organization. Wants skills that capture practical techniques, not session narratives.
§
User prefers concise, direct responses and values verification of changes. Prefers NixOS configuration work without Home Manager. Likes class-level skill organization. Wants skills that capture practical techniques, not session narratives.
§
User prefers using 'hermes config set' for individual configuration changes rather than direct file edits due to security restrictions that block direct modifications to sensitive settings.
§
User appreciates when agents archive significant work products (like plans) with timestamps in a history/ directory for future reference, as demonstrated in the Assay planning session.
§
User prefers concise, direct responses and values verification of changes. Prefers class-level skill organization, NixOS configuration work without Home Manager, and expects both default.nix and associated test files (like *.assay.nix) to be added together for NixOS features. Wants skills that capture practical techniques, not session narratives.