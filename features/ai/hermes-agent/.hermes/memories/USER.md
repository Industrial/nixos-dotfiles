Tom builds AI/agent feature integrations in his NixOS dotfiles repo (/home/tom/.dotfiles). He has a strong mental model of where things belong in the repo structure and corrects course quickly with minimal wording ("Hold on. You did that wrong.") when the approach diverges. He prefers a controlled, incremental workflow: research docs first (Context7 MCP) → discuss findings → then implement. When scoping a step he says "Do nothing else" and means it — do exactly what was asked, no more. Once he says "implement", he wants all files written completely in one pass.
§
User prefers concise, direct responses and does not want verbose explanations. User wants AI to focus on practical results rather than verbose output. User prefers NixOS configuration work without Home Manager. User values class-level skills over flat skill lists.
§
User prefers class-level skill organization with rich SKILL.md files and references/ directory structure. User values concise, direct responses over verbose explanations. User prefers NixOS configuration work without Home Manager. User wants practical, reproducible techniques captured as skills, not session narratives. User gets frustrated with verbose explanations and prefers concise answers.