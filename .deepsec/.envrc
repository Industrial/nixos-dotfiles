# Nix shell: provides a working `claude` (pkgs.claude-code) and sets
# CLAUDE_CODE_EXECUTABLE for deepsec / @anthropic-ai/claude-agent-sdk.
# The npm-bundled claude binaries are dynamically linked for FHS distros
# and do not run on NixOS without this.
use flake
